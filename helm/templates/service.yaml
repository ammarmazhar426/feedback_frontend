apiVersion: v1
kind: Service
metadata:
  name: {{ include "feedback-frontend.fullname" . }}
  labels:
    {{- include "feedback-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "feedback-frontend.selectorLabels" . | nindent 4 }}
