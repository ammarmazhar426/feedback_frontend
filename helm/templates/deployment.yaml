apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "feedback-frontend.fullname" . }}
  labels:
    {{- include "feedback-frontend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "feedback-frontend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "feedback-frontend.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
